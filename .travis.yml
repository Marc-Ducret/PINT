language: node_js
node_js:
    - "6"
install:
    - npm install -g grunt-cli
    - npm install
script:
    - grunt release
    - grunt test
deploy:
    name: PINT WebApp
    provider: pages
    local_dir: build
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    on:
        branch: master